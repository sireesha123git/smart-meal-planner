import {sqliteTable,text,integer,primaryKey,index} from 'drizzle-orm/sqlite-core';
export const households=sqliteTable('households',{id:text('id').primaryKey(),code:text('code').notNull().unique(),name:text('name').notNull(),masterHash:text('master_hash').notNull(),cookHash:text('cook_hash').notNull(),settings:text('settings').notNull(),createdAt:text('created_at').notNull()});
export const sessions=sqliteTable('sessions',{tokenHash:text('token_hash').primaryKey(),householdId:text('household_id').notNull().references(()=>households.id),role:text('role').notNull(),expiresAt:text('expires_at').notNull()},t=>[index('idx_sessions_expiry').on(t.expiresAt)]);
export const plans=sqliteTable('plans',{householdId:text('household_id').notNull().references(()=>households.id),date:text('date').notNull(),slot:text('slot').notNull(),recipeId:text('recipe_id'),drySide:text('dry_side'),liquid:text('liquid'),confirmed:integer('confirmed').notNull().default(0),startedAt:text('started_at'),servedAt:text('served_at'),servedBy:text('served_by'),corrected:integer('corrected').notNull().default(0)},t=>[primaryKey({columns:[t.householdId,t.date,t.slot]}),index('idx_plans_household_date').on(t.householdId,t.date)]);
export const events=sqliteTable('events',{id:text('id').primaryKey(),householdId:text('household_id').notNull().references(()=>households.id),date:text('date').notNull(),kind:text('kind').notNull(),payload:text('payload').notNull(),createdAt:text('created_at').notNull()},t=>[index('idx_events_household_created').on(t.householdId,t.createdAt)]);
export const stock=sqliteTable('stock',{householdId:text('household_id').notNull().references(()=>households.id),item:text('item').notNull(),available:integer('available').notNull().default(0),updatedAt:text('updated_at').notNull()},t=>[primaryKey({columns:[t.householdId,t.item]})]);
export const serviceRecords=sqliteTable('service_records',{householdId:text('household_id').notNull().references(()=>households.id),date:text('date').notNull(),serviceId:text('service_id').notNull(),kind:text('kind').notNull(),servedAt:text('served_at').notNull(),servedBy:text('served_by').notNull(),corrected:integer('corrected').notNull().default(0)},t=>[primaryKey({columns:[t.householdId,t.date,t.serviceId]}),index('idx_service_records_household_date').on(t.householdId,t.date)]);
